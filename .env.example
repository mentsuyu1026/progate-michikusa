# Gemini APIキー(Google AI Studio で発行: https://aistudio.google.com/apikey)
# このファイルをコピーして .env を作り、自分のキーを入れる。
# .env は git にコミットしない(.gitignore 済み)。Vercel では環境変数に同名で設定する。
GEMINI_API_KEY=ここにキーを貼る

# 任意: 使うモデルを変えたい場合(既定は gemini-2.0-flash)
# GEMINI_MODEL=gemini-2.0-flash
